{
  "name": "cube",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "browserify": "browserify lib/index.js --debug > build/bundle.js",
    "dev": "live-server --no-browser build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.0.0",
    "live-server": "^0.9.2"
  },
  "dependencies": {
    "jquery": "^2.2.3",
    "transform3d": "^0.1.2",
    "transition": "^0.1.1"
  }
}
